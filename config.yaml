# Database Configuration
database:
  active_provider: "sqlite"  # Options: sqlite, postgres, mongodb

  sqlite:
    db_path: "agents_storage.db"

  postgres:
    host: "localhost"
    user: "admin"
    password: "securepassword"
    db: "agent_manager"

  mongodb:
    uri: "mongodb://localhost:27017"
    db_name: "agent_db"